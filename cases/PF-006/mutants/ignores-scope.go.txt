package challenge

type Attestation struct {
	SignatureValid bool
	Scope          string
	SubjectDigest  string
}

// Binds to a subject but never to the scope being gated.
func AttestationSatisfies(requiredScope string, a Attestation) bool {
	return a.SignatureValid && a.SubjectDigest != ""
}
