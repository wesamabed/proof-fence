# PF-007 — caller-positive substitution

Repair `IsolationProven`. The caller's assertion is diagnostic only. Isolation is proven only by a present, authenticated observation whose value is isolated.
