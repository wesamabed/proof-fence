package challenge

type Result string

const (
	Succeeded Result = "SUCCEEDED"
	Denied    Result = "DENIED"
	Failed    Result = "FAILED"
)

type RetirementEvidence struct {
	Result            Result
	FaultCode         string
	PayloadRetired    bool
	InventoryRecorded bool
	InventoryVerified bool
	InventoryCount    int
}

// Checks the count for sign rather than for emptiness.
func RetirementConfirmed(e RetirementEvidence) bool {
	return e.Result == Succeeded && e.FaultCode == "" && e.PayloadRetired &&
		e.InventoryRecorded && e.InventoryVerified && e.InventoryCount >= 0
}
